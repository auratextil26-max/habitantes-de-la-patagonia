const buttons=document.querySelectorAll('.lang-btn');const translated=document.querySelectorAll('[data-es][data-en]');const header=document.getElementById('floatingHeader');const hero=document.getElementById('inicio');const transitionSection=document.getElementById('transicion');const heroMedia=document.querySelector('.hero-media');const start=document.getElementById('startJourney');
function applyLanguage(lang){document.documentElement.lang=lang;translated.forEach(el=>el.textContent=el.dataset[lang]);buttons.forEach(btn=>btn.classList.toggle('active',btn.dataset.lang===lang));localStorage.setItem('aura-language',lang)}
buttons.forEach(btn=>btn.addEventListener('click',()=>applyLanguage(btn.dataset.lang)));applyLanguage(localStorage.getItem('aura-language')||'es');
const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
function updateHeader(){header.classList.toggle('visible',window.scrollY>hero.offsetHeight*.72)}
let ticking=false;function updateParallax(){heroMedia.style.transform=`translate3d(0,${window.scrollY*.12}px,0) scale(1.045)`;ticking=false}
window.addEventListener('scroll',()=>{updateHeader();if(!ticking){requestAnimationFrame(updateParallax);ticking=true}},{passive:true});updateHeader();start.addEventListener('click',()=>transitionSection.scrollIntoView({behavior:'smooth'}));
